{
  "name": "payment-processing-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy --stage dev --aws-profile payrix",
    "remove": "serverless remove --stage dev --aws-profile payrix",
    "offline": "serverless offline start --stage dev --aws-profile payrix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^4.8.0",
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-webpack": "^5.15.1",
    "webpack": "^5.98.0",
    "webpack-node-externals": "^3.0.0"
  }
}
